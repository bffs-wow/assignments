# 2026-08-22 — BFFS assignments reconciliation session

## Key facts learned (assignments repo, `bffs-wow/assignments`)

- **Reference kill set** lives in the read-only sheet `1mfRwq54y-3AZO4JgmvYitW6JW83YsAPycwigZYswR8Y`
  with the full serialized payload in cell `'SOO-Assigns-Import'!G1` (491 `*`-segments covering all
  SOO bosses). Format: `EVENT/OCC/ROLE/TIME/SPELLID/npc/name/tts/customIcon`. Captured at
  `ref/ref-payload.txt` + `ref/ref-payload-parsed.json`. **Never write to this sheet.**
- **Live sheet** = `1SqMdIVBKMYRfOaGw4TucVVPpjqm4kvXqEZJo6W1HWms`; roster bindings at
  `'MoP-Data-Assigns'!F:G`. Role tags exist in `G` dropdown but must have `F` player bindings.
- **Real Blackfuse pull** (`cBkxGRQCq4fKXYMv` fight 25, 0.85%): **Magnetic Crush is never cast**.
  Real events = Electrostatic Charge ×27 (~16s cadence; tank debuff → Fear Ward targets),
  Overload(shredder) ×11, Protective Frenzy ×14, Overcharge ×13, Shockwave Missile ×5 (late).
  Shredder `-16s` pre-calls are invalid past the first (gaps ~11s early, then bigger).
- **Validator now accepts lone occurrence `-1`** (pre-event/countdown sentinel used by the
  reference, e.g. `INSANE_CALCULATION/-1/SHAMAN`). Negative inside a list still rejected.
  Commit `bc52b83` on `fix/generator-canonical-events`.
- **WCL auth**: the app's `src/services/wcl.ts` uses client-credentials OAuth; `.env` has real
  creds (the "credentials missing or placeholder" warning is a red herring when calling
  `executeQuery` directly). Default export `WCLService`; methods `executeQuery`, `getEncounterEvents`,
  `getCommunityPulls`. `executeQuery` returns raw `data` (not `{data:...}`).
- **ROSTER DRIFT (ref vs live)**: ref binds DISC1=Marenjok, UHDK1=Emofive, BOOMIE1=Pwndruid,
  LOCK2=Miyokan; live binds DISC1=unfilled, UHDK1=Zurrash, BOOMIE1=Shiftyz, LOCK2=Sloptard.
  Always re-resolve reference roles to live bindings (DISC* → HOLYPRIEST1=Nutmeg).
- `GROUP_TAGS` expanded to full sheet vocabulary (16 tags incl. class tags) so PRIEST/RANGEDDPS
  group rows validate (commit earlier on `fix/generator-canonical-events`).
- GitHub PAT (`GITHUB_PERSONAL_ACCESS_TOKEN`) is **read-only** — cannot create issues in this repo.
  Remote embedded token also lacks create scope. Document improvements under `docs/` instead.
- Memory file lives at `.openhands/memory/2026-08-22.md`, session capture at
  `docs/working-session-2026-08-22.md`.